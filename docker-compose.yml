version: '3.8'

services:
  devin-design-gate:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/devin_orchestrator.db
    restart: unless-stopped
